{% extends 'braintree/payments/base.html.twig' %}
{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('request') }}
{% endblock %}
{% block card_template %}
    <div class="row invisible" id="PaymentRequestButton">
        <div class="input-group mb-3" id="request-creation-destroyable">
            <div class="input-group-prepend">
                <span class="input-group-text" id="amount-text">Amount</span>
            </div>
            <input type="number" aria-label="Price" class="form-control text-right" id="request-amount" value="{{ app.session.get('settings')['settings-item-price']}}">
            <div class="input-group-append">
                <span class="input-group-text">.00 €</span>
            </div>
            <button id="payment-request-button" type="button" class="ml-1 btn btn-success">Create Payment Request</button>
        </div>
    </div>
    <div class="row invisible" id="PaymentRequestHostedFields">
        <div class="badge badge-pill badge-secondary">
            Impossible to render the PaymentRequest component of the browser
        </div>
    </div>
{% endblock %}
{% block card_details_help %}
{% endblock %}
{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('request') }}
{% endblock %}
